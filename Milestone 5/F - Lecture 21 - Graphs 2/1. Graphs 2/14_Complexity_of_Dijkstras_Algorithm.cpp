/*
        Time Complexity of Dijkstra’s algorithm:

        The time complexity is also the same as that of Prim’s 
        algorithm, i.e., O(n​^2). This can be reduced by using 
        the same approaches as discussed in Prim’s algorithm’s 
        content.

*/